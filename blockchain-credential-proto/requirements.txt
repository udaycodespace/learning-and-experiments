# Core web framework
Flask==3.1.1
Flask-SQLAlchemy==3.1.1
Werkzeug==3.1.1

# Database (SQLite default, PostgreSQL support)
psycopg2-binary==2.9.10

# Cryptography (REQUIRED for RSA signatures)
cryptography==45.0.5

# Production WSGI server
gunicorn==23.0.0

# HTTP requests (IPFS)
requests==2.32.4

# Email validation
email-validator==2.2.0

# Dev only - NOT in production
# python-dotenv==1.0.1
